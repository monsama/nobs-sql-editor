{
  "name": "nobs-sql-editor",
  "version": "1.3.16",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "test": "node --test tests/ui/*.test.mjs",
    "test:browser": "node --test tests/browser/*.browser.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
